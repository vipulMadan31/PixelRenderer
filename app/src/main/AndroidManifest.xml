<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission
      android:name="android.permission.WRITE_EXTERNAL_STORAGE"
      android:maxSdkVersion="29" />

  <application
      android:icon="@mipmap/ic_launcher"
      android:label="@string/hellovk_activity"
      android:requestLegacyExternalStorage="true">

    <!-- 🔥 REMOVE wrong activity coming from merged manifest -->
    <activity
        android:name="com.example.pixelrenderer.MainActivity"
        tools:node="remove" />

    <!-- ✅ CORRECT activity -->
    <activity
        android:name="com.android.hellovk.VulkanActivity"
        android:exported="true"
        android:theme="@style/Application.Fullscreen"
        android:launchMode="singleTask"
        android:configChanges="orientation|screenSize|keyboardHidden">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

    </activity>

  </application>
</manifest>